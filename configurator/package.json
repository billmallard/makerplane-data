{
  "name": "makerplane-configurator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Accounts/auth + design storage API for the pyEfis configuration manager (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 migrations apply makerplane_configurator --remote",
    "migrate:local": "wrangler d1 migrations apply makerplane_configurator --local"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
